import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'eduardo.uresti.com',
  appName: 'miApp-2076513',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
